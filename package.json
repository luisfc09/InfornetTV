{
  "name": "infornet-tv",
  "version": "1.0.0",
  "private": true,
  "description": "InfornetTV — deploy de serviço único (backend Express serve a SPA).",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "npm --prefix backend ci --include=dev && npm --prefix backend run build && npm --prefix frontend ci --include=dev && npm --prefix frontend run build",
    "start": "npm --prefix backend run start"
  }
}
