# InfornetTV — Frontend

App de streaming da Infornet (a definir).

Stack ainda não escolhida. Candidatos: Vite + React + TypeScript (player HLS via
`hls.js`), consumindo a API em `../backend`.

> Placeholder — substituir quando o frontend for iniciado.
